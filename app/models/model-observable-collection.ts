import { ModelBase } from './model-base';
import { Observable } from 'rxjs/Rx';

export class ModelObservableCollection {
	private models: Array<Observable<ModelBase>>;

	find(id: string)
	{

	}

	all()
	{
		
	}

}